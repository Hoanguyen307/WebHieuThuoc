@model PagedList.IPagedList<Models.KhachHang>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Styles {
    <link href="~/Content/assets/css/common.css" rel="stylesheet" />
}

<div class="container-wrapper" style=" display: flex; flex-direction: column;">
    <input type="hidden" id="Id" name="Id" />
    <div class="d-flex justify-content-between mb-3" style="padding: 0px 10px 0 260px;">
        <h4>Danh sách khách hàng</h4>
        <div>
            <button class="btn btn-outline-success">Excel operations <i class="fa fa-download"></i></button>
            <button type="button" class="btn btn-primary" onclick="LoadForm()">
                <i class="fas fa-plus"></i>
                Thêm mới
            </button>
        </div>
    </div>
    <form id="filterForm" class="filter-form">

        <input type="text" class="form-control form-control-sm search-input"
               id="searchString" name="searchString"
               placeholder="Tìm kiếm theo tên khách hàng..." />

        <!-- Tháng -->
        <select id="month" name="Month" class="form-select form-select-sm" style=" width: 95px !important;">
            <option value="">Tháng</option>
            @foreach (var m in ViewBag.Months as SelectList)
            {
                <option value="@m.Value">@m.Text</option>
            }
        </select>

        <!-- Năm -->
        <select id="year" name="Year" class="form-select form-select-sm" style=" width: 75px !important;">
            <option value="">Năm</option>
            @foreach (var y in ViewBag.Years as SelectList)
            {
                <option value="@y.Value">@y.Text</option>
            }
        </select>

        <!-- Nút lọc -->
        <button class="btn btn-primary" type="submit" id="searchBtn">
            <i class="fa fa-search"></i> Lọc
        </button>
    </form>

    <div class="main-content" style="flex:1; overflow: auto;">
        <div style=" overflow-x: auto;">
            <table id="ds-khachhang" class="table table-striped table-bordered" style="width: 100%; table-layout: auto; white-space: nowrap;">
                <thead class="thead-light">
                    <tr>
                        <th><input type="checkbox" id="SelectAll" /></th>
                        <th>STT</th>
                        <th>Họ và tên</th>
                        <th>Giới tính</th>
                        <th>Ngày sinh</th>
                        <th>Địa chỉ</th>
                        <th>Số điện thoại</th>
                        <th>Email</th>
                        <th>Số điểm hiện tại</th>
                        <th>Trạng thái</th>
                        <th>Hành động</th>
                    </tr>
                </thead>
                <tbody id="khachhang-body">
                </tbody>
            </table>
        </div>
        <div id="pagination" class="d-flex justify-content-end mt-2"></div>
    </div>

    <!--<div class="history-fixed">
        <ul class="nav nav-tabs" id="tabKhachHang" role="tablist">
            <li class="nav-item">
                <a class="nav-link active" id="lichsu-tab" data-toggle="tab" href="#lichsu" role="tab">Lịch sử đơn hàng</a>
            </li>-->
            @*<li class="nav-item">
                    <a class="nav-link" id="khac-tab" data-toggle="tab" href="#khac" role="tab">Thông tin khác</a>
                </li>*@
        <!--</ul>
        <div>
            <div class="tab-content mt-3" id="tabContentKhachHang">
                <div class="tab-pane fade show active" id="lichsu" role="tabpanel">
                    <table class="table table-striped table-bordered">
                        <thead class="thead-light">
                            <tr>
                                <th>STT</th>
                                <th>Mã đơn</th>
                                <th>Ngày đặt</th>
                                <th>Trạng thái</th>
                                <th>Tổng tiền</th>
                                <th>Thanh toán</th>
                                <th>Địa chỉ</th>
                                <th>Ghi chú</th>
                            </tr>
                        </thead>
                        <tbody id="lichSuBody">
                            <tr>
                                <td colspan="8" class="text-center">Vui lòng chọn khách hàng...</td>
                            </tr>
                        </tbody>
                    </table>
                </div>-->
                @*<div class="tab-pane fade" id="khac" role="tabpanel">
                        <p>Thông tin khác...</p>
                    </div>*@
            <!--</div>
        </div>
    </div>-->

    <div class="modal" id="modalKhachHang" style="color:black">
        <div class="modal-dialog modal-lg modal-dialog-scrollable" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modalKhachHangLabel">Thông tin khách hàng</h5>
                    <button type="button" onclick="closeModal()" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" style="color: black">
                </div>
            </div>
        </div>
    </div>

    <!-- Modal xem điểm -->
    <div class="modal fade" id="modalPointHistory" tabindex="-1" role="dialog" aria-labelledby="pointHistoryLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="pointHistoryLabel">Lịch sử điểm khách hàng</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>STT</th>
                                <th>Ngày giao dịch</th>
                                <th>Điểm thay đổi</th>
                                <th>Loại giao dịch</th>
                                <th>Mô tả</th>
                            </tr>
                        </thead>
                        <tbody id="pointHistoryTable">
                        </tbody>
                    </table>
                </div>
                <div id="pointPagination" class="d-flex justify-content-end mt-2"></div>
            </div>
        </div>
    </div>

</div>
@*<div id="loadingOverlay" style="
        display: none;
        position: fixed;
        top: 0; left: 0;
        width: 100vw;
        height: 100vh;
        background-color: rgba(255, 255, 255, 0.5);
        z-index: 2000;
        display: flex;
        align-items: center;
        justify-content: center;">
        <div style="margin-top: 20px; font-weight: bold; font-size: 18px; color: #333;">
            Đang xử lý...
        </div>
    </div>*@
@section Scripts {

    <script src="~/Content/assets/js/KhachHang.js?v=(@DateTime.Now.Ticks)"></script>
}

